{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Cite-Them-All Configuration",
  "description": "Default preferences for the cite-them-all academic reference agent",
  "type": "object",
  "properties": {
    "citation_format": {
      "type": "string",
      "enum": ["apa", "vancouver", "chicago", "harvard"],
      "description": "Citation style format"
    },
    "bibliography_location": {
      "type": "string",
      "enum": ["end_of_file", "separate_bib"],
      "description": "Where to place the bibliography"
    },
    "include_preprints": {
      "type": "boolean",
      "description": "Whether to search bioRxiv/medRxiv preprint servers (note: no keyword search available, browse by date+category only)"
    },
    "prefer_recent": {
      "type": "boolean",
      "description": "Prefer more recent publications when ranking candidates"
    },
    "recent_years": {
      "type": "integer",
      "minimum": 1,
      "maximum": 50,
      "description": "How many years back to prioritize"
    }
  },
  "required": ["citation_format", "bibliography_location"]
}
